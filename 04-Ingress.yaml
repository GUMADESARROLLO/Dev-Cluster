apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-app
  annotations :
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - http:
      paths:
      - path: /gumanet
        pathType: Prefix
        backend:
          service:
            name: sac-svc
            port:
              number: 80
